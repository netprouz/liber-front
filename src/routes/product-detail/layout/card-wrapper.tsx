import styled from 'styled-components';
import { COLORS } from 'config/styles-config';

export const CardWrapper = styled.div`
  border-radius: 10px;
  border: 0.7px solid ${COLORS.stroke};
  padding: 1.5rem;
`;
